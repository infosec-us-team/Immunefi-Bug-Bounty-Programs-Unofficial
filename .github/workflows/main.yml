on:
  workflow_dispatch:
  schedule:
    - cron: "0 1 * * *"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run script file
        run: |
           chmod +x ./scrapper.sh
           ./scrapper.sh
        shell: bash
